<%= semantic_form_for @user do |f| %>
  <h1 class="first">Personal Info</h1>
  <%= f.input :name %>
  <%# f.input :username %>
  <%= f.input :email, :label => ft(:email)+'*' %>
  <%= f.input :city %>
  <h1>Security</h1>
  <%= f.input :password %>
  <%= f.input :password_confirmation %>
  <%= f.submit (@user.new_record? ? t(:sign_up) : update(:profile)), :class => :submit %>
<% end %>
