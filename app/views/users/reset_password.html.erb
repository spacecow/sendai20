<%= semantic_form_for @user do |f| %>
  <%= f.input :old_password if current_user %>
  <%= f.input :password, :label => ft(:new_password) %>
  <%= f.input :password_confirmation, :label => ft(:new_password_confirmation) %>
<% end %>
